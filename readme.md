# monocap

Simple wrapper around OpenCV, handles platform detection as normally  
the OpenCV should handle the platform automatically, however in most cases it just throws errors when trying to use wrong backend.

Build
-----------------------------------------------------------------------------------
```bash
git clone https://github.com/Sora-3e8/monocap && cd build
cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake --build .
  
```
